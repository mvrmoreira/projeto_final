{% extends '::base.html.twig' %}

{% form_theme edit_form 'UffCalculatorBundle:Form:fields.html.twig' %}

{% block body -%}
    <h1>Instance edit</h1>

    {{ form(edit_form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('environment_show', { 'id': entity.environment.id }) }}">
                Back to environment
            </a>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
